<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <h1 class="text-center text-3xl font-bold mb-4">Editar Cliente</h1>
        
        <form action="/api/clientes/edit/<%= cliente.id %>" method="POST" class="max-w-lg mx-auto bg-white p-6 shadow-md rounded-lg">
            <div class="mb-4">
                <label for="nombre" class="block text-sm font-semibold text-gray-700">Nombre</label>
                <input type="text" name="nombre" id="nombre" class="w-full p-2 mt-2 border border-gray-300 rounded-md" value="<%= cliente.nombre %>" required>
            </div>

            <div class="mb-4">
                <label for="correo" class="block text-sm font-semibold text-gray-700">Correo</label>
                <input type="email" name="correo" id="correo" class="w-full p-2 mt-2 border border-gray-300 rounded-md" value="<%= cliente.correo %>" required>
            </div>

            <div class="mb-4 text-center">
                <button type="submit" class="bg-yellow-500 text-white px-6 py-2 rounded-lg">Actualizar Cliente</button>
            </div>
        </form>

        <div class="text-center mt-4">
            <a href="/api/clientes" class="bg-gray-500 text-white px-6 py-2 rounded-lg">Volver a la Lista de Clientes</a>
        </div>
    </div>
</body>
</html>
